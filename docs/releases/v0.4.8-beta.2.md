# v0.4.8-beta.2

- Release type: release-governance correction for `v0.4.8-beta.1`
- Release source SHA: release packet commit for `v0.4.8-beta.2`
- Previous prerelease: `v0.4.8-beta.1`
- Implementation PRs included in live source: `#186`, `#187`
- Tracking issues: `#184`, `#185`
- Live checkout: `/Volumes/LEXAR/repos/evaos-code-review-bot`
- launchd label: `com.electricsheephq.evaos-code-review-bot`
- Live config: `/Volumes/LEXAR/Codex/evaos-code-review-bot/config/active-installed-live.json`
- State DB: `/Volumes/LEXAR/Codex/evaos-code-review-bot/state/reviews-live.sqlite`
- Evidence path: `/Volumes/LEXAR/Codex/evaos-code-review-bot/evidence/2026-07-03/v0.4.8-beta.2/`
- Monitor owner/window: implementing agent, post-promotion heartbeat and open-head audit
- Rollback SHA: `v0.4.7-beta.1`
- Rollback command:

```bash
cd /Volumes/LEXAR/repos/evaos-code-review-bot
git fetch origin --tags
git checkout main
git reset --hard v0.4.7-beta.1
launchctl kickstart -k gui/$(id -u)/com.electricsheephq.evaos-code-review-bot
npx tsx src/cli.ts release-status \
  --config /Volumes/LEXAR/Codex/evaos-code-review-bot/config/active-installed-live.json \
  --expected-head "$(git rev-parse HEAD)" \
  --launchd-label com.electricsheephq.evaos-code-review-bot
```

## Purpose

Correct the `v0.4.8-beta.1` release surface after PR `#187` merged into `main`
between the initial release packet draft and tag publication. This release does
not add runtime behavior beyond the already-merged desktop cleanup and theme
work; it exists so the current live source is tied to accurate release notes,
evidence, and rollback instructions.

## Included Changes

- PR `#186`: NeonDiff Desktop CLI launch hardening, GUI-safe environment
  allowlisting, config redaction hardening, nested config patch documentation,
  and regression coverage.
- PR `#187`: Saiba-inspired NeonDiff Desktop SwiftUI development theme with
  shared theme tokens, angular panels, scanline/grid styling, themed sidebar and
  detail surfaces, and status badges.

## Gates

- `npm test`: 43 files passed, 590 tests passed.
- `npm run build`: passed.
- `release-status-final.json` from `v0.4.8-beta.1`: `runtime_ok`, launchd
  running with `NODE_OPTIONS=--use-system-ca`, zero failed queue jobs, zero
  provider cooldown backlog, zero stale review leases.
- `coverage-audit-post.json` from `v0.4.8-beta.1`: 20 repos scanned, 127 PRs
  seen, 111 processed, 0 queued, 0 unprocessed, 0 provider-deferred, 0 stale
  heads.
- `doctor-post.json` from `v0.4.8-beta.1`: ok, GitHub read mode
  `app_installation`.
- `provider-cooldowns-expired-post.json` from `v0.4.8-beta.1`: 0 expired
  cooldown rows.

## Caveats

- PR `#186` final head `d3d5d6a565d3de70b1faf93ff89cd56a9063edd3` did not
  receive terminal evaOS exact-head review proof before merge. Earlier heads
  were reviewed and later fast-follow heads were retired as stale.
- PR `#187` final head `1500da7753d981a6d8ffa40559315ab4eabb6520` was recorded
  as `closed_or_merged_before_review` after merge.
- These are governance/release-gate caveats. Post-promotion live runtime health
  is green, but the self-repo exact-head gate should be enforced more strongly
  in future release-critical merges.

## Runtime Notes

- This release does not widen monitored repos, issue enrichment allowlists,
  live concurrency, posting policy, ZCode tools, GitHub App permissions, or
  finishing-touch mutation behavior.
- The live default remains one active review run.
- Follow-up reliability work remains in `#170` for provider-throttle telemetry.
